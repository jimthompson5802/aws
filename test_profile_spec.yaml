# Test YAML specification with profile field
profile: "default"  # Using default profile which should exist

instances:
  - name: "test-web-server"
    instance_type: "t3.micro"
    ami_id: "ami-0c02fb55956c7d316"
    market_type: "on-demand"
    tags:
      - Key: "Environment"
        Value: "test"
    volumes:
      - size: 10
        type: "gp3"
        device: "/dev/sdf"
        encrypted: true
